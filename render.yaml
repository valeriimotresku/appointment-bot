services:
  - type: web
    name: appointment-bot
    env: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install --with-deps
    startCommand: |
      bash run.sh
    plan: free
    autoDeploy: true
    disk:
      name: data
      mountPath: /app
      sizeGB: 1

